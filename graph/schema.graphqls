type Ship {
  id: ID!
  name: String!
  description: String!
}

input NewShip {
  name: String!
  description: String!
}

type Query {
  ships: [Ship!]!
  ship(id: ID!): Ship!
}

type Mutation {
  createShip(input: NewShip!): Ship!
}